apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-hello-world
  labels:
    app: java-hello-world
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-hello-world
  template:
    metadata:
      labels:
        app: java-hello-world
    spec:
      containers:
        - name: java-hello-world
          image: bandhu8/javahelloworld:latest
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: log-volume
              mountPath: /usr/local/tomcat/logs
      volumes:
        - name: log-volume
          hostPath:
            path: /var/log/tomcat-logs